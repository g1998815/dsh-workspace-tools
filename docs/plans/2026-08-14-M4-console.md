# M4 控制台（PTY 终端）实现计划（dsh-workspace-tools）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 底部自绘控制台面板（可拖高、可收起）：多标签 PTY 终端（每标签一个会话），xterm 渲染、WS 输出流、RPC 输入；修复 M1 遗留的 WS client 帧解析 hack（换真实 RFC6455 解析器）与会话退出未清理问题。

**Architecture:** host 端 console 服务与 RPC 端点（M1 Task 6 / M2 Task 1）已就绪，本次只完善 WS 泵：新纯函数 `lib/services/ws-frames.js`（RFC6455 client→server 帧解析：masked、16/64 位长、text/ping/pong/close、分片简化处理）TDD，泵内替换 `replace(/^\x81.\x00?/,"")` hack，并补 stale-prune（会话自然退出时 `sessions.delete`）。client：`@xterm/xterm@6` + `@xterm/addon-fit@0.11` 作为 dependencies **打进 client bundle**（build.mjs 从 external 移除 `@xterm/*`，`loader: {".css":"text"}` 内联 xterm.css 并在模块加载时注入 `<style>`）；`src/components/console.js` 底部面板（fixed bottom、顶部拖高把手、开关按钮在右侧 rail 标题栏）+ 多标签（每标签 `console.create` 会话 + `Terminal` + `FitAddon` + WebSocket 输出流 + `console.write` RPC 输入 + `console.kill` 关闭）。

**Tech Stack:** Node.js ≥ 22（ESM）、`node:test`、esbuild（`text` loader 内联 css）、React 18 + `react/jsx-runtime`、`@xterm/xterm@6.0.0` + `@xterm/addon-fit@0.11.0`、`ctx.subprocess.spawnTerminal`（M1 §12 验证：SIGTERM→graceMs→SIGKILL）。

**设计决策（本计划定稿，覆盖 M1/M2 遗留）：**
- **WS 协议定稿**：client→host 发 **masked 二进制帧** `{"sessionId":"<tty-id>"}` 首帧（后续输入走 RPC `console.write`，不发 WS 文本）；host→client 发**非 masked 文本帧**（PTY 输出原文）+ JSON 文本帧 `{"type":"exit"}`（退出通知）。复用现有 `frame()` 发送器（已支持 16/64 位长）。
- **入站解析**：`createFrameParser()` 纯函数（unmask、len 126/127、text/ping/pong/close、分片仅累积文本 continuation——M4 client 只发单帧，分片支持为完备性）；不 masked 的帧丢弃（协议要求 client 帧必须 masked）。
- **stale prune（M1 遗留修复）**：`onExit` 时 `sessions.delete(sessionId)`（当前只 frame exit + cleanup，Map 里残留僵尸会话）。
- **resize 限制（M1 §12）**：`spawnTerminal` 无 resize API → spawn 时固定 rows/cols（40×120）；client `FitAddon` 只影响显示，PTY 不随之 resize（M4 接受此限制，动态 resize 需 node-pty 直连，列为 M4+ 备选；计划注明）。
- **Windows**：`spawnTerminal` 在 win32 必抛 → console.create 返回结构化错误（UI 显示"Windows 暂不支持控制台"）；真机验证列验收项。
- **底部面板落点**：`position:fixed bottom:0 left:0 right:0`（shell.overlay 浮层内自绘，z-index 15 < rail 的 100？不——rail 是 5，窗口 100。底部面板 z-index 12，让右侧 rail（5）在其上）；开关按钮放右侧 rail 标题栏（`data-wt-console-toggle`）；高度拖拽把手（顶部 4px，clamp 120–480px）；收起时仅剩把手条。
- **xterm css**：`@xterm/xterm/css/xterm.css` 经 esbuild `text` loader 打包为字符串，console 模块加载时 `document.head.appendChild(style)`（一次性）。

## Global Constraints

- 仓库位置：`/Volumes/data/code/dsh-workspace-tools`。
- client 束契约：`client.js` 已入库，改动后必须重建提交；build.mjs 为 CJS wrapper 形态；`react`/`react/jsx-runtime`/`@deepseek-ai/*` external——**`@xterm/*` 不再 external（打包进 bundle）**；src 内不 import @deepseek-ai/*。
- RPC 契约（M2 定稿）：handler `(endpoint, payload)`；`{ok:true,value}` / `{ok:false,error}` 信封；`sessionId` 必带（fail-closed）。
- `console.create` 端点已带 guardCwd + `createShellSession` DI（`ctx.subprocess.spawnTerminal`）；`console.write`/`kill` 已信封化（`session-not-found` 用枚举内 code）。
- npm 安装环境 fallback（沙箱 EPERM/ERESOLVE）：`npm install --no-audit --no-fund --legacy-peer-deps --cache "$TMPDIR/dsh-npm-cache"`。
- 测试基线：79 全绿。M4 预计新增：Task 1 ws-frames ≈ +6、Task 4 可选 → 终态 ≈ 85。
- **host 改动需重启 harness**（WS 泵在 lib/index.js）；client.js 刷新即生效。
- 环境事实：node 不在 PATH（`export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH` 前缀）；harness @ 3080。

---

### Task 1: host WS 帧解析器（RFC6455 client→server）+ 泵改造 + stale prune

**Files:**
- Create: `lib/services/ws-frames.js`
- Create: `test/ws-frames.test.js`
- Modify: `lib/index.js`（WS 泵入站解析替换 + onExit 清理）

**Interfaces:**
- Produces:
  - `createFrameParser() → { push(chunk) → Array<{type:"text"|"ping"|"pong"|"close", fin, payload?}> }`（Buffer 入，事件出；内部缓冲不完整帧；不 masked 帧丢弃；len 126/127 支持；文本 payload utf8 解码）
- Consumes: 现有 `frame()` 发送器（不动）。

- [ ] **Step 1: 写失败测试**

```js
// test/ws-frames.test.js
import test from "node:test";
import assert from "node:assert/strict";
import { createFrameParser } from "../lib/services/ws-frames.js";

// client→server 帧必须 masked：构造一个 masked 文本帧
function maskedText(text, maskKey = [1, 2, 3, 4]) {
  const payload = Buffer.from(text, "utf8");
  const masked = Buffer.alloc(payload.length);
  for (let i = 0; i < payload.length; i++) masked[i] = payload[i] ^ maskKey[i % 4];
  const head = Buffer.from([0x81, 0x80 | payload.length, ...maskKey]);
  return Buffer.concat([head, masked]);
}

test("parse: 单帧 masked 文本（<126）", () => {
  const p = createFrameParser();
  const out = p.push(maskedText('{"sessionId":"tty-1"}'));
  assert.equal(out.length, 1);
  assert.equal(out[0].type, "text");
  assert.equal(out[0].fin, true);
  assert.equal(out[0].payload, '{"sessionId":"tty-1"}');
});

test("parse: 缓冲跨块（帧被拆成两段 push）", () => {
  const p = createFrameParser();
  const buf = maskedText("hello world");
  const a = p.push(buf.subarray(0, 5));
  assert.equal(a.length, 0);
  const b = p.push(buf.subarray(5));
  assert.equal(b.length, 1);
  assert.equal(b[0].payload, "hello world");
});

test("parse: 126 长度（≥126 字节）", () => {
  const p = createFrameParser();
  const text = "x".repeat(200);
  const payload = Buffer.from(text, "utf8");
  const masked = Buffer.alloc(payload.length);
  for (let i = 0; i < payload.length; i++) masked[i] = payload[i] ^ (i % 4 + 1);
  const head = Buffer.alloc(8);
  head[0] = 0x81; head[1] = 0x80 | 126; head.writeUInt16BE(payload.length, 2);
  for (let i = 0; i < 4; i++) head[4 + i] = i + 1;
  const out = p.push(Buffer.concat([head, masked]));
  assert.equal(out.length, 1);
  assert.equal(out[0].payload, text);
});

test("parse: 不 masked 的帧被丢弃", () => {
  const p = createFrameParser();
  const out = p.push(Buffer.from([0x81, 0x03, 0x61, 0x62, 0x63])); // 未 masked 文本 "abc"
  assert.equal(out.length, 0);
});

test("parse: 多帧一次 push 全部产出", () => {
  const p = createFrameParser();
  const out = p.push(Buffer.concat([maskedText("a"), maskedText("b")]));
  assert.deepEqual(out.map((o) => o.payload), ["a", "b"]);
});

test("parse: close 帧", () => {
  const p = createFrameParser();
  const out = p.push(Buffer.from([0x88, 0x82, 1, 2, 3, 4, 0x03 ^ 1, 0xe8 ^ 2])); // masked close + 2 字节载荷
  assert.equal(out.length, 1);
  assert.equal(out[0].type, "close");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --test test/ws-frames.test.js`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现**

```js
// lib/services/ws-frames.js —— RFC6455 client→server 帧解析（masked、16/64 位长、text/ping/pong/close、分片文本累积）
export function createFrameParser() {
  let buffer = Buffer.alloc(0);
  let pending = null; // {fin, payloads: []} 分片文本累积
  return { push };

  function push(chunk) {
    buffer = Buffer.concat([buffer, chunk]);
    const out = [];
    while (true) {
      if (buffer.length < 2) break;
      const b0 = buffer[0];
      const b1 = buffer[1];
      const fin = (b0 & 0x80) !== 0;
      const opcode = b0 & 0x0f;
      const masked = (b1 & 0x80) !== 0;
      let len = b1 & 0x7f;
      let offset = 2;
      if (len === 126) {
        if (buffer.length < 4) break;
        len = buffer.readUInt16BE(2);
        offset = 4;
      } else if (len === 127) {
        if (buffer.length < 10) break;
        len = Number(buffer.readBigUInt64BE(2));
        offset = 10;
      }
      // 协议要求 client→server 帧必须 masked；未 masked 整帧丢弃
      if (!masked) {
        buffer = buffer.subarray(offset + len);
        continue;
      }
      if (buffer.length < offset + 4 + len) break;
      const maskKey = buffer.subarray(offset, offset + 4);
      const payload = Buffer.alloc(len);
      for (let i = 0; i < len; i++) payload[i] = buffer[offset + 4 + i] ^ maskKey[i % 4];
      buffer = buffer.subarray(offset + 4 + len);
      if (opcode === 0x1 || opcode === 0x0) {
        // 文本帧或 continuation
        if (pending) pending.payloads.push(payload);
        else if (opcode === 0x1) pending = { fin, payloads: [payload] };
        else continue; // 孤立 continuation 忽略
        if (pending && fin) {
          out.push({ type: "text", fin: true, payload: Buffer.concat(pending.payloads).toString("utf8") });
          pending = null;
        }
      } else if (opcode === 0x8) {
        out.push({ type: "close", fin: true });
      } else if (opcode === 0x9) {
        out.push({ type: "ping", fin: true, payload });
      } else if (opcode === 0xa) {
        out.push({ type: "pong", fin: true, payload });
      }
      // 其他 opcode（binary/0x2 等）忽略
    }
    return out;
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --test test/ws-frames.test.js`
Expected: 6 个用例 PASS。

- [ ] **Step 5: lib/index.js WS 泵改造**

把 socket `"data"` 监听（当前 `JSON.parse(buf.toString("utf8").replace(/^\x81.\x00?/, ""))` hack）替换为：

```js
        // 入站帧解析（RFC6455，client→server 必须 masked）——M4 替换 M1 hack
        const parser = createFrameParser();
        socket.on("data", (buf) => {
          for (const ev of parser.push(buf)) {
            if (ev.type === "text") {
              try {
                const msg = JSON.parse(ev.payload);
                if (msg.sessionId) attach(msg.sessionId);
              } catch {
                /* 非 JSON 帧忽略 */
              }
            } else if (ev.type === "close") {
              cleanup();
            }
          }
        });
```

`onExit` 补 stale prune（`sessions.delete`）：

```js
          const onExit = () => {
            frame(JSON.stringify({ type: "exit" }));
            sessions.delete(sessionId); // M4：自然退出清理（M1 遗留）
            cleanup();
          };
```

import 更新：`import { createFrameParser } from "./services/ws-frames.js";`

注意：`attach` 里 `cleanup` 与 socket close 的引用顺序——`onExit` 引用的 `cleanup` 在其后定义（现有代码同结构，保持）。

- [ ] **Step 6: 语法 + 全量测试**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --check lib/index.js && node --check lib/services/ws-frames.js && node --test`
Expected: check 通过；85 全绿（79 + 6）。

- [ ] **Step 7: Commit**

```bash
git add lib/services/ws-frames.js test/ws-frames.test.js lib/index.js
git commit -m "feat: RFC6455 WS client-frame parser + stale session prune (M4 console)"
```

---

### Task 2: xterm 依赖接入（npm install + build.mjs css 内联）

**Files:**
- Modify: `package.json`（dependencies 加 `@xterm/xterm@^6.0.0`、`@xterm/addon-fit@^0.11.0`）
- Modify: `build.mjs`（external 移除 `@xterm/*`；`loader: {".css":"text"}`）
- Modify: `src/index.js`（apply 顶部注入 xterm css——或由 console 组件注入，见 Task 3；本任务仅打通构建）
- Verify: `client.js` 重建含 xterm 代码

**Interfaces:**
- Produces: `npm install` 后 node_modules 含 xterm；`node build.mjs` 把 xterm 打进 client.js（体积明显增大）；css text 可 import。

- [ ] **Step 1: 安装依赖**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && npm install --no-audit --no-fund --legacy-peer-deps --cache "$TMPDIR/dsh-npm-cache" @xterm/xterm@^6.0.0 @xterm/addon-fit@^0.11.0`
Expected: 安装成功；`package.json` dependencies 出现两项；node_modules/@xterm/{xterm,addon-fit} 存在。

- [ ] **Step 2: 改 build.mjs**

- `external` 数组保持 `["react", "react/jsx-runtime", "@deepseek-ai/*"]`（xterm 会打包）。
- build 配置加：`loader: { ".css": "text" },`（css 以文本字符串打包）。
- 其余不变（CJS wrapper 逻辑）。

- [ ] **Step 3: src 里验证 css 可导入 + 注入**

在 `src/components/console.js`（Task 3 建）实现时用：
```js
import xtermCss from "@xterm/xterm/css/xterm.css";
```
模块加载时（factory 闭包内）：
```js
if (typeof document !== "undefined" && !document.getElementById("dshwt-xterm-css")) {
  const s = document.createElement("style");
  s.id = "dshwt-xterm-css";
  s.textContent = xtermCss;
  document.head.appendChild(s);
}
```

- [ ] **Step 4: 构建验证**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node build.mjs`
Expected: 构建成功，client.js 显著增大（含 xterm）；`grep -c "xterm" client.js` ≥1（或 "dshwt-xterm-css" 注入代码在）。

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json build.mjs
git commit -m "build: bundle @xterm/xterm + addon-fit (css text-inline)"
```

> 注：`package-lock.json` 之前被 .gitignore 排除（含私有 peer 记录）——本任务不提交 lock（保持 ignore），只提交 package.json 与 build.mjs。若安装产生 lock 变更，不 stage 它。

---

### Task 3: client 底部控制台面板（多标签 + xterm + WS + RPC 输入）

**Files:**
- Create: `src/components/console.js`（ConsolePanel：底部面板 + 标签 + xterm 生命周期）
- Modify: `src/components/workspace-browser.js`（rail 标题栏加控制台开关按钮 + 渲染 ConsolePanel）
- Modify: `client.js`（重建）

**Interfaces:**
- Produces:
  - `ConsolePanel({ cwd, sessionId, rpc, open, onToggle })`：底部 fixed 面板（展开/收起、顶部拖高把手 clamp 120–480）
  - 每标签：`{tabId, ttyId, status}`；新建 → `console.create {cwd, sessionId}` → ttyId → 建 Terminal+FitAddon+WS
  - WS：`new WebSocket(\`ws://\${location.host}/plugins/dsh-workspace-tools/console\`)` → onopen 发 masked 文本帧 `{"sessionId":ttyId}`（浏览器 WebSocket 自动 masked ✓）→ onmessage（文本帧 → JSON.parse 若 `{type:"exit"}` 则标记退出；否则 term.write）→ onclose 标记
  - 输入：`term.onData(data)` → `callRpc("console.write", {cwd, sessionId, sessionId: ttyId, data})`（注意 payload 键：console.write 的 sessionId 是 ttyId）
  - 关闭标签：`console.kill {sessionId: ttyId}` + WS close + term.dispose
  - exit 后：状态显示"已退出" + 可"重开"（新建会话）
- Consumes: `callRpc`、`console.create/write/kill` 端点、WS_PATH 常量（lib/constants.js 的 WS_PATH）。

- [ ] **Step 1: 写 ConsolePanel（结构契约 + 关键代码）**

```js
// src/components/console.js —— 底部控制台面板：多标签 PTY（xterm + WS 输出 + RPC 输入）
import { jsx } from "react/jsx-runtime";
import { useCallback, useEffect, useRef, useState } from "react";
import { Terminal } from "@xterm/xterm";
import { FitAddon } from "@xterm/addon-fit";
import xtermCss from "@xterm/xterm/css/xterm.css";
import { callRpc } from "../lib/rpc.js";
import { WS_PATH } from "../../lib/constants.js";

// xterm css 注入（一次性）
if (typeof document !== "undefined" && !document.getElementById("dshwt-xterm-css")) {
  const s = document.createElement("style");
  s.id = "dshwt-xterm-css";
  s.textContent = xtermCss;
  document.head.appendChild(s);
}

const MIN_H = 120;
const MAX_H = 480;
const ROWS = 40;
const COLS = 120;

export function ConsolePanel({ cwd, sessionId, rpc, open, onToggle, onCountChange }) {
  const [height, setHeight] = useState(280);
  const [tabs, setTabs] = useState([]); // [{id, ttyId, status: "running"|"exited", title}]
  const [active, setActive] = useState(null); // tab id
  const [panelError, setPanelError] = useState(null);
  const mountRefs = useRef({}); // tabId -> {term, ws, el}
  const seq = useRef(0);

  // 新建标签
  const addTab = useCallback(() => {
    if (!cwd) { setPanelError("当前会话没有工作目录"); return; }
    const id = `c${++seq.current}`;
    const tab = { id, ttyId: null, status: "starting", title: `终端 ${tabs.length + 1}` };
    setTabs((prev) => [...prev, tab]);
    setActive(id);
    setPanelError(null);
    callRpc(rpc, "console.create", { cwd, sessionId })
      .then((value) => {
        setTabs((prev) => prev.map((t) => (t.id === id ? { ...t, ttyId: value.sessionId, status: "running" } : t)));
      })
      .catch((err) => {
        setTabs((prev) => prev.map((t) => (t.id === id ? { ...t, status: "exited" } : t)));
        setPanelError(String(err?.message ?? err)); // Windows 等：spawnTerminal 不支持 → 结构化错误
      });
  }, [cwd, rpc, sessionId, tabs.length]);

  // 每个 running 标签：mount xterm + WS
  useEffect(() => {
    for (const tab of tabs) {
      if (tab.status !== "running" || !tab.ttyId) continue;
      if (mountRefs.current[tab.id]) continue; // 已挂
      const el = document.getElementById(`dshwt-term-${tab.id}`);
      if (!el) continue;
      const term = new Terminal({ rows: ROWS, cols: COLS, fontSize: 12, theme: { background: "#141414" } });
      const fit = new FitAddon();
      term.loadAddon(fit);
      term.open(el);
      try { fit.fit(); } catch { /* 忽略 */ }
      const ws = new WebSocket(`${location.protocol === "https:" ? "wss" : "ws"}://${location.host}${WS_PATH}`);
      ws.binaryType = "arraybuffer";
      ws.onopen = () => {
        // 浏览器 WebSocket 发送自动 masked ✓；首帧带 ttyId
        ws.send(JSON.stringify({ sessionId: tab.ttyId }));
      };
      ws.onmessage = (ev) => {
        const text = typeof ev.data === "string" ? ev.data : new TextDecoder().decode(ev.data);
        try {
          const msg = JSON.parse(text);
          if (msg.type === "exit") {
            setTabs((prev) => prev.map((t) => (t.id === tab.id ? { ...t, status: "exited" } : t)));
            term.write("\r\n\x1b[31m[会话已退出]\x1b[0m\r\n");
            return;
          }
        } catch { /* 非 JSON = PTY 输出 */ }
        term.write(text);
      };
      ws.onclose = () => {
        setTabs((prev) => prev.map((t) => (t.id === tab.id && t.status === "running" ? { ...t, status: "exited" } : t)));
      };
      term.onData((data) => {
        if (tab.ttyId) callRpc(rpc, "console.write", { cwd, sessionId, sessionId: tab.ttyId, data }).catch(() => {});
      });
      mountRefs.current[tab.id] = { term, ws };
    }
    return () => {
      // 组件卸载：清理全部（RPC kill 由各标签关闭处理；此处 dispose 所有 term/ws）
      for (const [id, m] of Object.entries(mountRefs.current)) {
        m.ws?.close();
        m.term?.dispose();
        delete mountRefs.current[id];
      }
    };
    // 注意：本 effect 依赖 tabs 变化增量挂载；卸载清理只在组件卸载（open=false 卸载）时执行。
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [tabs, cwd, rpc, sessionId]);

  // 关闭标签
  const closeTab = useCallback((tab) => {
    if (tab.ttyId) {
      callRpc(rpc, "console.kill", { sessionId: tab.ttyId }).catch(() => {});
    }
    mountRefs.current[tab.id]?.ws?.close();
    mountRefs.current[tab.id]?.term?.dispose();
    delete mountRefs.current[tab.id];
    setTabs((prev) => prev.filter((t) => t.id !== tab.id));
    setActive((prev) => (prev === tab.id ? null : prev));
  }, [rpc]);

  // 重开：新建标签并激活（保留 tab 结构——直接 addTab）
  const reopen = useCallback(() => addTab(), [addTab]);

  // 拖高把手
  const onDragDown = useCallback((e) => {
    const startY = e.clientY;
    const startH = height;
    const move = (ev) => setHeight(Math.min(MAX_H, Math.max(MIN_H, startH + (startY - ev.clientY))));
    const up = () => { window.removeEventListener("mousemove", move); window.removeEventListener("mouseup", up); };
    window.addEventListener("mousemove", move);
    window.addEventListener("mouseup", up);
  }, [height]);

  if (!open) return null; // 由父组件条件渲染（收起=卸载，会话不 kill——见 Step 2 修正）

  return jsx("div", {
    "data-wt-console": true,
    style: {
      position: "fixed", left: 0, right: 0, bottom: 0, height, zIndex: 12,
      background: "#141414", borderTop: "1px solid var(--dsw-alias-border-l2, #333)",
      display: "flex", flexDirection: "column",
    },
    children: [
      jsx("div", { "data-wt-console-drag": true, onMouseDown: onDragDown, style: { height: 4, cursor: "row-resize", flexShrink: 0 } }),
      jsx("div", { "data-wt-console-bar": true, style: { display: "flex", alignItems: "center", gap: 6, padding: "4px 8px", borderBottom: "1px solid var(--dsw-alias-border-l2, #333)", flexShrink: 0 }, children: [
        jsx("span", { style: { fontSize: 12, fontWeight: 600, color: "var(--dsw-alias-text-secondary, #999)" }, children: "控制台" }),
        jsx("div", { style: { display: "flex", gap: 4, flex: 1, overflow: "auto" }, children: tabs.map((t) =>
          jsx("span", { key: t.id, "data-wt-console-tab": true, "data-active": active === t.id || undefined, onClick: () => setActive(t.id), style: { fontSize: 12, padding: "2px 10px", borderRadius: 4, cursor: "pointer", background: active === t.id ? "var(--dsw-alias-fill-hover, rgba(255,255,255,0.08))" : "none", display: "flex", gap: 6, alignItems: "center" }, children: [
            t.title,
            t.status === "exited" && jsx("span", { style: { color: "#e06c75", fontSize: 10 }, children: "已退出" }),
            jsx("span", { role: "button", "data-wt-console-close": true, onClick: (e) => { e.stopPropagation(); closeTab(t); }, style: { cursor: "pointer", color: "var(--dsw-alias-text-secondary, #999)" }, children: "✕" }),
          ]),
        )),
        jsx("button", { type: "button", "data-wt-console-new": true, onClick: addTab, style: { background: "none", border: "1px solid var(--dsw-alias-border-l2, #444)", borderRadius: 4, color: "var(--dsw-alias-text-secondary, #999)", cursor: "pointer", fontSize: 12, padding: "0 8px" }, children: "+" }),
        jsx("button", { type: "button", "data-wt-console-collapse": true, onClick: onToggle, style: { background: "none", border: "none", cursor: "pointer", color: "var(--dsw-alias-text-secondary, #999)", fontSize: 12 }, children: "▾" }),
      ] }),
      panelError && jsx("div", { "data-wt-console-error": true, style: { padding: "4px 10px", color: "#e06c75", fontSize: 12, borderBottom: "1px solid var(--dsw-alias-border-l2, #333)" }, children: panelError }),
      jsx("div", { style: { flex: 1, minHeight: 0, position: "relative", display: "flex" }, children: tabs.map((t) =>
        jsx("div", {
          key: t.id,
          id: `dshwt-term-${t.id}`,
          "data-wt-console-term": true,
          style: { flex: 1, minWidth: 0, display: active === t.id ? "block" : "none", position: "relative", padding: 4 },
        }),
      ) }),
      tabs.length === 0 && jsx("div", { style: { flex: 1, display: "flex", alignItems: "center", justifyContent: "center", color: "var(--dsw-alias-text-secondary, #666)", fontSize: 12 }, children: "点击 + 新建终端" }),
    ],
  });
}
```

> 注意：`console.write` 的 payload 键是 `sessionId`（ttyId 复用同键）；`console.kill` 同。RPC 输入不传 cwd（端点不需要——write/kill 只查 sessions Map）。代码中 `callRpc(rpc, "console.write", { cwd, sessionId, sessionId: tab.ttyId, data })` 的重复键是笔误——**实现时用 `{ sessionId: tab.ttyId, data }`**（console.write 端点只读 payload.sessionId）。

- [ ] **Step 2: workspace-browser.js 接线**

- `RightSidebar` 加状态 `const [consoleOpen, setConsoleOpen] = useState(false);`
- rail 标题栏（或页签条右侧）加按钮 `data-wt-console-toggle`（"终端"）→ `setConsoleOpen(v => !v)`
- rail 渲染末尾**常驻**渲染：`jsx(ConsolePanel, { cwd, sessionId: current, rpc, open: consoleOpen, onToggle: () => setConsoleOpen(false) })`（不条件卸载）
- import ConsolePanel。

> **设计修正（防丢会话）**：ConsolePanel **常驻渲染**，`open` 只控制 `style.display`（展开 flex / 收起 none）——收起面板**不卸载组件、不 kill 会话**，标签与 PTY 会话保留，重开立即恢复。组件卸载（不会发生）时才 dispose 全部。因此 ConsolePanel 内部 `if (!open) return null` 改为根容器 `style: { display: open ? "flex" : "none", ... }`。ConsolePanel 是 fixed bottom，渲染在 rail 内无妨（fixed 脱离定位）。

- [ ] **Step 3: 语法 + 构建 + 全量测试**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --check src/components/console.js && node --check src/components/workspace-browser.js && node build.mjs && node --test`
Expected: check 通过；构建成功（client.js 显著增大，含 xterm）；85 全绿。

- [ ] **Step 4: 静态验证**

Run: `grep -c "data-wt-console" client.js && grep -c "dshwt-xterm-css" client.js && grep -c "xterm" client.js && head -3 client.js`
Expected: 均 ≥1；首行 `window.__ModuleLoader__.load({`。

- [ ] **Step 5: Commit**

```bash
git add src/components/console.js src/components/workspace-browser.js client.js
git commit -m "feat: M4 console panel (multi-tab PTY, xterm + WS output + RPC input)"
```

---

### Task 4: 集成验证与交付

**Files:**
- Modify: `docs/plans/2026-08-14-M4-console.md`（勾选）
- 无新代码。

- [ ] **Step 1: 全量测试 + 构建**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --test && node build.mjs`
Expected: 85 全绿；构建成功。

- [ ] **Step 2: 重启 harness（host WS 泵改动生效）+ 探测**

- 确认插件注册 → 重启（`node scripts/install.mjs` 若丢失先重装）→ 等 3080 up
- curl `client.js` 200 且含 xterm/console 标记
- curl 探测 `console.create`（live 会话）→ 返回 ttyId（**POSIX 有效**；Windows 必抛结构化错误属预期）→ `console.kill`
- WS 探测（可选）：node 脚本连 ws://127.0.0.1:3080/plugins/.../console，发 masked 首帧，收输出

- [ ] **Step 3: GUI 手动验收**

- [ ] 右侧 rail"终端"按钮 → 底部面板出现；拖顶部把手调高；"▾"收起
- [ ] "+" 新建标签 → xterm 渲染，出现 shell 提示符（`cd /Volumes/data/code/dsh-workspace-tools` 验证 cwd）
- [ ] 输入命令（`echo hello` / `ls`）→ 输出回显
- [ ] 多标签切换；关闭标签 → 会话 kill
- [ ] `exit` 命令 → 标签显示"已退出"；"重开"可用
- [ ] Windows 真机：console.create 报结构化错误（验收项，若环境允许）

- [ ] **Step 4: 推送 GitHub**

```bash
git add docs/plans/2026-08-14-M4-console.md
git commit -m "docs: M4 console plan accepted"
TOKEN=$(grep -oE 'github_pat_[A-Za-z0-9_]+' ~/.dsh/profiles/web/cordis.patch.yml | head -1)
git push "https://x-access-token:${TOKEN}@github.com/g1998815/dsh-workspace-tools.git" main:main
```

---

## 里程碑验收清单（M4）

- [ ] `node --test` 全绿（85 用例）。
- [ ] WS 泵用 RFC6455 解析器（unmask/长帧/close），会话退出清理（Task 1 单测覆盖）。
- [ ] xterm + fit 打进 client bundle，css 内联注入（构建验证）。
- [ ] 底部面板：开关、拖高、多标签、+ 新建、✕ 关闭、▾ 收起。
- [ ] PTY 交互：输出回显、输入生效、exit 处理、cwd=工作区。
- [ ] 推送 GitHub main。

## 后续（非 M4 范围）

- 动态 resize（spawnTerminal 无 resize API；node-pty 直连为 M4+ 备选，Windows 同）；Windows PTY 真机验证（spawnTerminal win32 必抛，列为验收项）；xterm 主题/字体设置；控制台滚动回溯（xterm scrollback 默认）。

- **运维提醒**：console 是独立 PTY（owner=本插件），与 agent bash 工具互不干扰；WS 仅 loopback。
