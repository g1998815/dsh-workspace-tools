# M3c 变更页签 v2 + 交互增强 实现计划（dsh-workspace-tools）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 变更页签升级为上下双分区（上=未提交：checkbox 多选 + 单个/全部提交 + 提交前预览 + 分支显示 + 最近消息复用；下=已提交：历史列表 + 安全回退 + 已推送风险提示），分隔条拖拽调高；侧边栏宽度拖拽；页签变更数徽章；文件树过滤；文本预览行号 + 轻量语法高亮；diff/预览浮窗抽公共 DraggableWindow。

**Architecture:** 4 个任务。Task 1 host 首次引入 git 写操作（用户 2026-08-15 裁定）：`git.log`（历史+分支）、`git.commit`（`add -- <files>` / `add -A` + `commit -m`）、`git.reset`（**仅 `--mixed`** 安全回退）、`git.branch`（当前分支）；纯函数 `lib/services/git-history.js` 复用 git-diff.js 导出的 `runGit`，TDD。Task 2 抽公共 `DraggableWindow`（可拖拽标题栏 + 关闭 + 受控搜索条），diff/preview 两个浮窗重构复用（行为等价，重构风险由审查兜底）。Task 3 变更页签 v2：上下分区（分隔条拖拽）+ checkbox 提交 + 预览选中（多文件 diff 拼接进 DiffWindow）+ 历史/回退（两段式确认 + 风险提示）+ 分支显示 + 最近消息下拉 + 页签计数徽章；rail 宽度拖拽。Task 4 文件树顶部过滤框（纯函数 `filterRows`）+ 文本预览行号与轻量语法高亮（纯函数 `tokenize`）。

**Tech Stack:** Node.js ≥ 22（ESM）、`node:test`、esbuild、React 18 + `react/jsx-runtime`、git CLI（`-c core.quotepath=false`）。

**设计决策（本计划定稿，含用户 2026-08-15 裁定）：**
- **git 写操作边界**：`git.commit`/`git.reset` 走 `guardCwd`（fail-closed）；`git.reset` 仅 `--mixed`（HEAD 回退、工作区改动保留，无数据丢失；用户裁定"仅安全回退"），target 校验 `/^[0-9a-f]{4,40}$/`；`git.commit` message 走 `execFile` 数组参数（无 shell 注入面），files 逐文件 `add --`。
- **分支显示（增强 1）**：`git.branch` → `{branch}`（`git branch --show-current`，空=detached）；变更页签顶部 `📌 main`。
- **提交前预览（增强 2）**：勾选后"预览"→ 对每个选中文件 `git.getDiff` → `parseDiff` 拼接（文件间插入 `--- <path> ---` 分隔 meta 行）→ 复用 DiffWindow 渲染；提交按钮旁显示 `n 个文件`。
- **最近消息复用（增强 3）**：`git.log` limit 5 的 subject 存为 recent；消息框"▾"下拉点击填入。
- **回退风险提示（增强 4）**：选中提交时提示"回退会改写本地历史；若该提交已推送，重新推送需 force"。
- **变更数徽章（增强 5）**：Changes 通过 `onCountChange(n)` 回调 → RightSidebar 页签 label 显示 `变更{n}`（n>0 时）。
- **文件树过滤（增强 6）**：纯函数 `filterRows(rows, q)`（name/path 大小写不敏感包含），顶部输入框，空 q 不过滤。
- **公共 DraggableWindow（增强 7）**：`position:fixed` + 标题栏拖拽 + 关闭 + 受控搜索条（value/onChange/onEnter/onPrev/onNext/count/active），子组件自管数据与滚动定位；diff/preview 重构复用（行为等价）。
- **预览行号 + 轻量高亮（增强 8）**：`tokenize(text, ext) → [{text, cls}]`（字符串/注释/关键字/数字着色，按语言组：js 组、py、json），预览行加行号列。
- 页签顺序不变（文件→变更→会话）；分隔条拖拽 clamp 20–80%；rail 宽度 clamp 200–600px。

## Global Constraints

- 仓库位置：`/Volumes/data/code/dsh-workspace-tools`。
- client 束契约：`client.js` 已入库，改动后必须重建提交；build.mjs 为 CJS wrapper 形态；`react`/`react/jsx-runtime`/`@deepseek-ai/*` external；src 内不 import @deepseek-ai/*。
- RPC 契约（M2 定稿）：handler `(endpoint, payload)`；`{ok:true,value}` / `{ok:false,error}` 信封；`sessionId` 必带；错误 code 仅限预置枚举（`failFrom` 映射）。
- git 命令一律 `-c core.quotepath=false`；`execFile` 数组参数。
- 测试基线：60 全绿。M3c 预计新增：Task 1 ≈ +8、Task 3 relativeTime +5、Task 4 ≈ +6 → 终态 ≈ 79。
- **host 改动需重启 harness 生效**（重启后插件注册若丢失重跑 `node scripts/install.mjs`）；client.js 刷新即生效。
- 环境事实：node 不在 PATH（`export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH` 前缀）。

---

### Task 1: host git 端点（log/commit/reset/branch 纯函数 + RPC）

**Files:**
- Modify: `lib/services/git-diff.js`（导出 `runGit`/`gitBase`）
- Create: `lib/services/git-history.js`
- Create: `test/git-history.test.js`
- Modify: `lib/index.js`（新增 `git.log`/`git.commit`/`git.reset`/`git.branch` 四个 case）

**Interfaces:**
- Produces:
  - `logCommits(cwd, {limit=50}) → Promise<{commits: Array<{hash, shortHash, subject, author, date}>}>`
  - `commitAll(cwd, message, files?) → Promise<{hash}>`（files 缺省 → `add -A`；有 → `add -- <files…>`；无可提交 → reject `{code:"nothing-to-commit"}`）
  - `resetTo(cwd, target) → Promise<{ok:true}>`（仅 `--mixed`；target 须 `/^[0-9a-f]{4,40}$/`，否则 `{code:"invalid-target"}`）
  - `currentBranch(cwd) → Promise<{branch}>`（`git branch --show-current`，空串=detached）
- Consumes: `runGit`（git-diff.js 导出）、`stat`。

- [ ] **Step 1: git-diff.js 导出 runGit/gitBase**（仅加 `export` 关键字，行为不变）

- [ ] **Step 2: 写失败测试**

```js
// test/git-history.test.js
import test from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, writeFileSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { execFileSync } from "node:child_process";
import { logCommits, commitAll, resetTo, currentBranch } from "../lib/services/git-history.js";

function makeRepo() {
  const dir = mkdtempSync(join(tmpdir(), "dshwt-gh-"));
  const git = (args, cwd = dir) => execFileSync("git", ["-c", "core.quotepath=false", ...args], { cwd, stdio: "pipe" });
  git(["init", "-q"]);
  git(["config", "user.email", "t@t"]);
  git(["config", "user.name", "t"]);
  writeFileSync(join(dir, "a.txt"), "one\n");
  git(["add", "."]);
  git(["commit", "-qm", "first commit"]);
  return { dir, git, cleanup: () => rmSync(dir, { recursive: true, force: true }) };
}

test("logCommits: 新到旧排序，含完整字段", async () => {
  const repo = makeRepo();
  try {
    writeFileSync(join(repo.dir, "a.txt"), "one\ntwo\n");
    repo.git(["add", "."]);
    repo.git(["commit", "-qm", "second commit"]);
    const { commits } = await logCommits(repo.dir, { limit: 10 });
    assert.equal(commits.length, 2);
    assert.equal(commits[0].subject, "second commit");
    assert.match(commits[0].hash, /^[0-9a-f]{40}$/);
    assert.equal(commits[0].shortHash, commits[0].hash.slice(0, 7));
    assert.equal(commits[0].author, "t");
    assert.ok(!Number.isNaN(Date.parse(commits[0].date)));
  } finally {
    repo.cleanup();
  }
});

test("commitAll: 全部提交", async () => {
  const repo = makeRepo();
  try {
    writeFileSync(join(repo.dir, "b.txt"), "hi\n");
    writeFileSync(join(repo.dir, "a.txt"), "one\nchanged\n");
    const { hash } = await commitAll(repo.dir, "commit all");
    assert.match(hash, /^[0-9a-f]{40}$/);
    const { commits } = await logCommits(repo.dir);
    assert.equal(commits[0].subject, "commit all");
  } finally {
    repo.cleanup();
  }
});

test("commitAll: 指定文件只提交选中", async () => {
  const repo = makeRepo();
  try {
    writeFileSync(join(repo.dir, "b.txt"), "hi\n");
    writeFileSync(join(repo.dir, "c.txt"), "c\n");
    await commitAll(repo.dir, "commit b", ["b.txt"]);
    const out = execFileSync("git", ["-c", "core.quotepath=false", "status", "--porcelain"], { cwd: repo.dir, encoding: "utf8" });
    assert.match(out, /\?\? c\.txt/);
  } finally {
    repo.cleanup();
  }
});

test("commitAll: 无可提交 → nothing-to-commit", async () => {
  const repo = makeRepo();
  try {
    await assert.rejects(commitAll(repo.dir, "noop"), (e) => e.code === "nothing-to-commit");
  } finally {
    repo.cleanup();
  }
});

test("resetTo: mixed 回退保留工作区改动", async () => {
  const repo = makeRepo();
  try {
    writeFileSync(join(repo.dir, "a.txt"), "one\ntwo\n");
    repo.git(["add", "."]);
    repo.git(["commit", "-qm", "second"]);
    const before = await logCommits(repo.dir);
    await resetTo(repo.dir, before.commits[1].hash);
    const after = await logCommits(repo.dir);
    assert.equal(after.commits.length, 1);
    assert.equal(after.commits[0].subject, "first commit");
    const content = execFileSync("cat", [join(repo.dir, "a.txt")], { encoding: "utf8" });
    assert.match(content, /two/);
  } finally {
    repo.cleanup();
  }
});

test("resetTo: 非法 target 拒绝", async () => {
  const repo = makeRepo();
  try {
    await assert.rejects(resetTo(repo.dir, "abc;rm -rf"), (e) => e.code === "invalid-target");
    await assert.rejects(resetTo(repo.dir, "zzz"), (e) => e.code === "invalid-target");
  } finally {
    repo.cleanup();
  }
});

test("currentBranch: 提交后返回分支名，init 未提交时为空", async () => {
  const repo = makeRepo();
  try {
    const { branch } = await currentBranch(repo.dir);
    assert.ok(branch.length > 0);
  } finally {
    repo.cleanup();
  }
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --test test/git-history.test.js`
Expected: FAIL（模块不存在）。

- [ ] **Step 4: 写实现**

```js
// lib/services/git-history.js —— git 历史/提交/安全回退/分支（复用 git-diff.js 的 runGit）
import { stat } from "node:fs/promises";
import { runGit } from "./git-diff.js";

const TARGET_RE = /^[0-9a-f]{4,40}$/;

async function ensureRepo(cwd) {
  await stat(cwd).catch(() => {
    throw { code: "dir-not-found", message: `目录不存在: ${cwd}` };
  });
}

export async function logCommits(cwd, { limit = 50 } = {}) {
  await ensureRepo(cwd);
  const out = await runGit(cwd, ["log", `-n ${limit}`, "--format=%H%x1f%h%x1f%s%x1f%an%x1f%aI"]);
  const commits = out
    .split("\n")
    .filter((line) => line !== "")
    .map((line) => {
      const [hash, shortHash, subject, author, date] = line.split("\x1f");
      return { hash, shortHash, subject, author, date };
    });
  return { commits };
}

export async function commitAll(cwd, message, files) {
  await ensureRepo(cwd);
  if (files && files.length > 0) {
    await runGit(cwd, ["add", "--", ...files]);
  } else {
    await runGit(cwd, ["add", "-A"]);
  }
  const names = await runGit(cwd, ["diff", "--cached", "--name-only"]);
  if (names.trim() === "") throw { code: "nothing-to-commit", message: "没有可提交的变更" };
  const out = await runGit(cwd, ["commit", "-m", message]);
  const m = /[0-9a-f]{40}/.exec(out);
  const hash = m ? m[0] : (await runGit(cwd, ["rev-parse", "HEAD"])).trim();
  return { hash };
}

export async function resetTo(cwd, target) {
  await ensureRepo(cwd);
  if (typeof target !== "string" || !TARGET_RE.test(target)) {
    throw { code: "invalid-target", message: `非法回退目标: ${target}` };
  }
  await runGit(cwd, ["reset", "--mixed", target]);
  return { ok: true };
}

export async function currentBranch(cwd) {
  await ensureRepo(cwd);
  const out = await runGit(cwd, ["branch", "--show-current"]);
  return { branch: out.trim() };
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --test test/git-history.test.js`
Expected: 7 个用例 PASS（若 `--format`/hash 提取有偏差，按测试实际输出修正——TDD 以测试为准）。

- [ ] **Step 6: lib/index.js 追加四个 case**（`git.getDiff` 之后）+ import：

```js
            case "git.log": {
              const guardError = guardCwd(payload);
              if (guardError) return guardError;
              return ok(await logCommits(payload.cwd, { limit: payload.limit ?? 50 }));
            }
            case "git.branch": {
              const guardError = guardCwd(payload);
              if (guardError) return guardError;
              return ok(await currentBranch(payload.cwd));
            }
            case "git.commit": {
              const guardError = guardCwd(payload);
              if (guardError) return guardError;
              if (typeof payload.message !== "string" || payload.message.trim() === "") {
                return fail("bad-request", "提交消息不能为空", { issues: [] });
              }
              return ok(await commitAll(payload.cwd, payload.message, payload.files));
            }
            case "git.reset": {
              const guardError = guardCwd(payload);
              if (guardError) return guardError;
              return ok(await resetTo(payload.cwd, payload.target));
            }
```

`import { logCommits, commitAll, resetTo, currentBranch } from "./services/git-history.js";`

- [ ] **Step 7: 语法 + 全量测试**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --check lib/index.js && node --check lib/services/git-history.js && node --test`
Expected: check 通过；67 全绿（60 + git-history 7）。

- [ ] **Step 8: Commit**

```bash
git add lib/services/git-diff.js lib/services/git-history.js test/git-history.test.js lib/index.js
git commit -m "feat: git endpoints (log/commit/reset --mixed/branch) with safe validation"
```

---

### Task 2: 公共 DraggableWindow + 浮窗重构

**Files:**
- Create: `src/components/draggable-window.js`
- Modify: `src/components/diff-window.js`、`src/components/preview-window.js`（重构复用）
- Modify: `client.js`（重建）

**Interfaces:**
- Produces: `DraggableWindow({ title, badge?, width=640, onClose, search?, children })`
  - 内聚：`position:fixed` 容器 + pos state + 标题栏拖拽（input/button 除外）+ ✕ 关闭
  - `search?: { value, onChange, onEnter, onPrev, onNext, count, active }`——受控搜索条渲染（输入 + n/m + ↑/↓），滚动定位由子组件自管（子组件持有 bodyRef + matches）
- 行为等价：diff/preview 浮窗的拖拽/搜索/关闭体验不变（重构，非新功能）。

- [ ] **Step 1: 写 DraggableWindow**

```js
// src/components/draggable-window.js —— 可拖拽浮窗外壳（标题栏 + 关闭 + 可选受控搜索条）
import { jsx } from "react/jsx-runtime";
import { useCallback, useState } from "react";

export function DraggableWindow({ title, badge, width = 640, onClose, search, children }) {
  const [pos, setPos] = useState(() => {
    const vw = typeof window !== "undefined" ? window.innerWidth : 1200;
    return { x: Math.max(8, vw - width - 24), y: 64 };
  });
  const dragRef = useRefState();
  const onTitleDown = useCallback(
    (e) => {
      if (e.target.closest("input,button")) return;
      dragRef.current = { dx: e.clientX - pos.x, dy: e.clientY - pos.y };
      const move = (ev) => setPos({ x: Math.max(0, ev.clientX - dragRef.current.dx), y: Math.max(0, ev.clientY - dragRef.current.dy) });
      const up = () => { window.removeEventListener("mousemove", move); window.removeEventListener("mouseup", up); };
      window.addEventListener("mousemove", move);
      window.addEventListener("mouseup", up);
    },
    [pos],
  );
  const hasQuery = search != null && search.value.trim() !== "";
  return jsx("div", {
    "data-wt-window": true,
    style: {
      position: "fixed", left: pos.x, top: pos.y, width, maxWidth: "94vw", height: "68vh", minHeight: 240,
      display: "flex", flexDirection: "column", background: "var(--dsw-alias-bg-base, #1a1a1a)",
      border: "1px solid var(--dsw-alias-border-l2, #333)", borderRadius: 8,
      boxShadow: "0 8px 32px rgba(0,0,0,0.5)", zIndex: 100, fontSize: 12, overflow: "hidden",
    },
    children: [
      jsx("div", {
        "data-wt-window-title": true,
        onMouseDown: onTitleDown,
        style: { display: "flex", alignItems: "center", gap: 8, padding: "6px 10px", cursor: "move", background: "var(--dsw-alias-bg-float, #1f1f1f)", borderBottom: "1px solid var(--dsw-alias-border-l2, #333)", flexShrink: 0, userSelect: "none" },
        children: [
          jsx("span", { style: { fontWeight: 600, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: title }),
          badge && jsx("span", { style: { fontSize: 10, padding: "1px 5px", border: "1px solid #888", borderRadius: 3, color: "#aaa", flexShrink: 0 }, children: badge }),
          hasQuery && jsx("span", { "data-wt-window-count": true, style: { color: "#e6b450", fontSize: 11, whiteSpace: "nowrap", flexShrink: 0 }, children: search.count }),
          jsx("button", { type: "button", "data-wt-window-close": true, onClick: onClose, title: "关闭（Esc）", style: { marginLeft: "auto", background: "none", border: "none", color: "var(--dsw-alias-text-secondary, #999)", cursor: "pointer", fontSize: 14, padding: "0 4px", flexShrink: 0 }, children: "✕" }),
        ],
      }),
      search &&
        jsx("div", { style: { display: "flex", gap: 6, padding: "5px 10px", borderBottom: "1px solid var(--dsw-alias-border-l2, #333)", flexShrink: 0, alignItems: "center" }, children: [
          jsx("input", {
            "data-wt-window-search": true,
            type: "text",
            placeholder: "搜索…",
            value: search.value,
            onChange: (e) => search.onChange(e.target.value),
            onKeyDown: (e) => {
              if (e.key === "Enter") search.onEnter(e);
              if (e.key === "Escape") onClose();
            },
            style: { flex: 1, background: "var(--dsw-alias-bg-base, #141414)", border: "1px solid var(--dsw-alias-border-l2, #333)", borderRadius: 4, color: "var(--dsw-alias-text-primary, #ddd)", padding: "3px 8px", fontSize: 12, outline: "none" },
          }),
          hasQuery && jsx("button", { type: "button", "data-wt-window-prev": true, onClick: search.onPrev, style: BTN, children: "↑" }),
          hasQuery && jsx("button", { type: "button", "data-wt-window-next": true, onClick: search.onNext, style: BTN, children: "↓" }),
        ] }),
      children,
    ],
  });
}
// BTN 为按钮样式常量（背景透明/边框/圆角/光标），实现时内联定义。
```

> 注：`useRefState` 是占位名——实现时用标准 `useRef(null)`；`BTN` 为内联样式常量。行为与现有 diff/preview 浮窗一致。

- [ ] **Step 2: 重构 diff-window.js / preview-window.js 复用 DraggableWindow**

- diff-window：保留 `matches` 计算与滚动定位（bodyRef 在子内容区）；标题/状态徽章/搜索控件改为传 `search={{value: query, onChange: setQuery, onEnter: (e)=>e.shiftKey?prevMatch():nextMatch(), onPrev, onNext, count: matches.length?`${Math.min(matchIdx+1,matches.length)}/${matches.length}`:"0/0", active: hasQuery}}`；`data-wt-diff-window`/`data-wt-diff-search` 等标记保留在相应子元素（静态验证依赖）。
- preview-window：同样迁移（kind 徽章、文本搜索）。
- 行为等价：重构后拖拽/搜索/关闭/滚动体验不变；`data-wt-diff-*`/`data-wt-preview-*` 标记保留。

- [ ] **Step 3: 语法 + 构建 + 全量测试**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --check src/components/draggable-window.js && node --check src/components/diff-window.js && node --check src/components/preview-window.js && node build.mjs && node --test`
Expected: check 通过；构建成功；67 全绿（无新单测）。

- [ ] **Step 4: 静态验证**

Run: `grep -c "data-wt-window" client.js && grep -c "data-wt-diff-search" client.js && grep -c "data-wt-preview-search" client.js && head -3 client.js`
Expected: 均 ≥1；首行 `window.__ModuleLoader__.load({`。

- [ ] **Step 5: Commit**

```bash
git add src/components/draggable-window.js src/components/diff-window.js src/components/preview-window.js client.js
git commit -m "refactor: shared DraggableWindow for diff/preview windows (behavior-preserving)"
```

---

### Task 3: 变更页签 v2（分区/提交/历史/回退/分支/消息/徽章）+ rail 拖拽

**Files:**
- Create: `src/lib/git-history-client.js`、`test/git-history-client.test.js`
- Modify: `src/components/changes.js`（大改）
- Modify: `src/components/workspace-browser.js`（rail 宽度拖拽 + 变更徽章）
- Modify: `client.js`（重建）

**Interfaces:**
- Produces: `relativeTime(iso, now?) → string`（纯函数）；Changes 新 prop `onCountChange?: (n:number)=>void`；RightSidebar 页签 label `变更{n}`、rail 宽度 state。
- Consumes: Task 1 RPC `git.log/git.branch/git.commit/git.reset`；`DiffWindow`（预览选中用）。

- [ ] **Step 1: 纯函数 + 测试**

```js
// src/lib/git-history-client.js
export function relativeTime(iso, now = Date.now()) {
  const t = Date.parse(iso);
  if (Number.isNaN(t)) return "";
  const diff = Math.max(0, now - t);
  const min = Math.floor(diff / 60000);
  if (min < 1) return "刚刚";
  if (min < 60) return `${min} 分钟前`;
  const hr = Math.floor(min / 60);
  if (hr < 24) return `${hr} 小时前`;
  const day = Math.floor(hr / 24);
  if (day < 30) return `${day} 天前`;
  const mon = Math.floor(day / 30);
  if (mon < 12) return `${mon} 个月前`;
  return `${Math.floor(mon / 12)} 年前`;
}
```

```js
// test/git-history-client.test.js
import test from "node:test";
import assert from "node:assert/strict";
import { relativeTime } from "../src/lib/git-history-client.js";
test("relativeTime: 时间档位", () => {
  const now = Date.parse("2026-08-15T12:00:00Z");
  assert.equal(relativeTime("2026-08-15T11:59:30Z", now), "刚刚");
  assert.equal(relativeTime("2026-08-15T11:55:00Z", now), "5 分钟前");
  assert.equal(relativeTime("2026-08-15T09:00:00Z", now), "3 小时前");
  assert.equal(relativeTime("2026-08-01T00:00:00Z", now), "14 天前");
  assert.equal(relativeTime("not-a-date", now), "");
});
```

Run: `node --test test/git-history-client.test.js` → 5 断言 1 用例 PASS。

- [ ] **Step 2: 重构 changes.js（结构契约）**

新增状态：`checked:Set<path>`、`commitMsg`、`showRecent:boolean`、`recentMessages:string[]`、`branch`、`commits`、`histStatus`、`histError`、`selCommit`、`confirmReset`、`splitPct(50)`、`previewLines`（预览选中的拼接 diff）。

布局（data-wt-changes，flex column，100%）：
1. 顶部信息行：`📌 {branch || "detached"}` + "↻ 刷新"
2. 上半区（style height `${splitPct}%`，minHeight 0，flexShrink 0）：
   - 工具行：消息输入（data-wt-commit-msg）+ "▾ 最近"（data-wt-recent，点击展开 recentMessages 下拉，选中填入 commitMsg）+ "提交选中(n)"（data-wt-commit-selected，disabled 当 n==0 或 msg 空）+ "全部提交"（data-wt-commit-all）+ "预览(n)"（data-wt-preview-selected，disabled 当 n==0）
   - 变更列表（现有 rows 渲染 + 每行行首 checkbox data-wt-check：`checked:checked.has(row.path)`，onChange toggle；行点击仍打开 diff 浮窗）
3. 分隔条（data-wt-split）：onMouseDown → 记录起始 y 与 splitPct → window mousemove 按容器高度（`e.clientY - rect.top`）/rect.height*100 → setSplitPct(clamp 20–80) → mouseup 清理
4. 下半区（flex 1，minHeight 0，overflow auto）：
   - 历史列表：每行 `shortHash · subject · relativeTime(date)`（data-wt-history-row，点击 → selCommit）
   - selCommit 时底部操作条：`回退到 {shortHash}`（data-wt-reset，默认灰；confirmReset 时变红"确认回退？"）+ 风险提示小字（data-wt-reset-warn："若该提交已推送，回退后重新推送需 force"）

关键接线：
- `load()`：并行 `git.listChanges` + `git.branch` + `git.log({limit:50})` → setGroups / setBranch / setCommits；`git.log({limit:5})` → setRecentMessages（recentMessages 单独或复用 commits.slice(0,5).map(subject)）；`onCountChange?.(changes.length)`
- `commitSelected()`：`git.commit {message, files:[...checked]}` → 成功：清 checked/commitMsg → load()；失败：`window` 内错误提示（复用 error 行）
- `commitAll()`：`git.commit {message}`（无 files）→ 同上
- `previewSelected()`：对每个 checked 文件依次 `git.getDiff {file, untracked:来自 changes}` → parseDiff 拼接（文件间插入 `{kind:"meta", text:`--- ${path} ---`}`）→ setPreviewLines → 打开 DiffWindow（复用现有 diff 浮窗渲染逻辑：可直接把 previewLines 喂给 DiffWindow 的 diffLines）
- `doReset()`：confirmReset 时二段确认；`git.reset {target: selCommit.hash}` → 清 selCommit/confirmReset → load()
- diff 浮窗（单文件点击）与预览选中浮窗共用 DiffWindow；`data-wt-diff` 渲染逻辑保留

（完整组件约 350 行；实现者按此结构 + 现有 changes.js 的 jsx 风格编写，渲染细节沿用 M3 模式。）

- [ ] **Step 3: workspace-browser.js（rail 拖拽 + 变更徽章）**

- `const [railW, setRailW] = useState(300);`；rail style `width: railW`；rail 首子元素改为 `data-wt-resize` 把手（width 4、cursor col-resize、onMouseDown → window mousemove：`setRailW(Math.min(600, Math.max(200, window.innerWidth - e.clientX)))` → mouseup 清理）
- 变更徽章：`const [changeCount, setChangeCount] = useState(0);`；TABS 渲染 label：`t.label` 替换为 `t.id === "changes" && changeCount > 0 ? \`变更 ${changeCount}\` : t.label`；`jsx(Changes, { ..., onCountChange: setChangeCount })`

- [ ] **Step 4: 语法 + 构建 + 全量测试**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --check src/components/changes.js && node --check src/components/workspace-browser.js && node build.mjs && node --test`
Expected: check 通过；构建成功；68 全绿（67 + relativeTime 1）。

- [ ] **Step 5: 静态验证**

Run: `grep -c "data-wt-split" client.js && grep -c "data-wt-resize" client.js && grep -c "data-wt-commit-selected" client.js && grep -c "data-wt-history-row" client.js && grep -c "git.reset" client.js`
Expected: 均 ≥1。

- [ ] **Step 6: Commit**

```bash
git add src/lib/git-history-client.js test/git-history-client.test.js src/components/changes.js src/components/workspace-browser.js client.js
git commit -m "feat: changes tab v2 (split+commit+history+reset, branch, recent msgs, count badge) + resizable rail"
```

---

### Task 4: 文件树过滤 + 预览行号/语法高亮

**Files:**
- Create: `src/lib/tree-filter.js`、`test/tree-filter.test.js`；`src/lib/tokenize.js`、`test/tokenize.test.js`
- Modify: `src/components/file-tree.js`（过滤框）、`src/components/preview-window.js`（行号 + 高亮）
- Modify: `client.js`（重建）

**Interfaces:**
- Produces:
  - `filterRows(rows, q) → rows`（name/path 大小写不敏感包含；空 q 原样返回）
  - `tokenize(text, ext) → Array<{text, cls}>`（cls ∈ "str"|"com"|"kw"|"num"|null；按语言组：js 组(js/ts/jsx/tsx/java)、py、json；关键字集各语言；字符串 `"..."`/`'...'`/`` `...` ``、注释 `//` `#` `<!--`、数字）

- [ ] **Step 1: 纯函数 + 测试**

```js
// src/lib/tree-filter.js
export function filterRows(rows, q) {
  const query = (q ?? "").trim().toLowerCase();
  if (!query) return rows;
  return rows.filter((r) => (r.name ?? "").toLowerCase().includes(query) || (r.path ?? "").toLowerCase().includes(query));
}
```

```js
// src/lib/tokenize.js —— 极简语法高亮 token 化（字符串/注释/关键字/数字）
const KEYWORDS = {
  js: new Set(["const","let","var","function","return","if","else","for","while","class","import","export","from","new","this","async","await","try","catch","throw","switch","case","break","continue","typeof","instanceof","extends","super","static","get","set","null","undefined","true","false"]),
  java: new Set(["public","private","protected","class","interface","extends","implements","return","if","else","for","while","new","this","static","final","void","int","long","double","boolean","String","try","catch","throw","import","package","null","true","false"]),
  py: new Set(["def","class","return","if","elif","else","for","while","import","from","as","with","try","except","finally","lambda","pass","break","continue","None","True","False","self","global","nonlocal","yield","raise","in","is","not","and","or"]),
};
export function tokenize(text, ext) {
  const lang = ext === "py" ? "py" : ext === "java" ? "java" : ext === "js" || ext === "ts" || ext === "jsx" || ext === "tsx" ? "js" : null;
  const kws = lang ? KEYWORDS[lang] : null;
  const out = [];
  const re = /("(?:[^"\\]|\\.)*"|'(?:[^'\\]|\\.)*'|`(?:[^`\\]|\\.)*`|\/\/[^\n]*|#[^\n]*|<!--[\s\S]*?-->|\b\d+(?:\.\d+)?\b|[A-Za-z_$][A-Za-z0-9_$]*)/g;
  let last = 0;
  let m;
  while ((m = re.exec(text)) !== null) {
    if (m.index > last) out.push({ text: text.slice(last, m.index), cls: null });
    const tok = m[0];
    let cls = null;
    if (tok.startsWith('"') || tok.startsWith("'") || tok.startsWith("`")) cls = "str";
    else if (tok.startsWith("//") || tok.startsWith("#") || tok.startsWith("<!--")) cls = "com";
    else if (/^\d/.test(tok)) cls = "num";
    else if (kws && kws.has(tok)) cls = "kw";
    out.push({ text: tok, cls });
    last = m.index + tok.length;
  }
  if (last < text.length) out.push({ text: text.slice(last), cls: null });
  return out;
}
```

```js
// test/tree-filter.test.js + test/tokenize.test.js
import test from "node:test";
import assert from "node:assert/strict";
import { filterRows } from "../src/lib/tree-filter.js";
import { tokenize } from "../src/lib/tokenize.js";

test("filterRows: 按 name/path 过滤，空 q 原样", () => {
  const rows = [
    { name: "a.js", path: "src/a.js" },
    { name: "b.md", path: "docs/b.md" },
    { name: "lib", path: "lib" },
  ];
  assert.equal(filterRows(rows, "A.JS").length, 1);
  assert.equal(filterRows(rows, "md").length, 1);
  assert.equal(filterRows(rows, "src").length, 1);
  assert.equal(filterRows(rows, "").length, 3);
  assert.equal(filterRows(rows, "zzz").length, 0);
});

test("tokenize: js 字符串/注释/关键字/数字", () => {
  const toks = tokenize('const s = "hi"; // note\nreturn 42;', "js");
  const cls = toks.map((t) => t.cls);
  assert.ok(cls.includes("kw"));
  assert.ok(cls.includes("str"));
  assert.ok(cls.includes("com"));
  assert.ok(cls.includes("num"));
  assert.ok(toks.some((t) => t.text === "const" && t.cls === "kw"));
});

test("tokenize: 未知语言不抛错且无关键字", () => {
  const toks = tokenize("hello world", "xyz");
  assert.ok(toks.length >= 1);
  assert.ok(toks.every((t) => t.cls === null));
});
```

- [ ] **Step 2: 跑测试确认失败 → 实现 → 通过**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --test test/tree-filter.test.js test/tokenize.test.js`
Expected: 先 FAIL（模块不存在）→ 实现后 4 个用例 PASS；全量 72（68 + 4）。

- [ ] **Step 3: file-tree.js 顶部过滤框**

- 新增 state `filterQ`；面板顶部（tabs 之下、树之上）渲染 `data-wt-filter` input（placeholder "过滤…"）；`const rows = useMemo(() => filterRows(visibleRows(nodes, expanded), filterQ), [nodes, expanded, filterQ]);`（替换现有 rows 计算）；根 loading/error 分支不变。

- [ ] **Step 4: preview-window.js 行号 + 高亮**

- 文本行渲染：行前加行号列（width 44，同 diff 风格，`data-wt-preview-lineno`）；行内容用 `tokenize(line, ext)` 渲染——每个 token 按 cls 着色（str→#7ec699、com→#6a737d、kw→#61afef、num→#e6b450），用 `<span style={{color}}>`；ext 从 `file.split(".").pop().toLowerCase()`。

- [ ] **Step 5: 语法 + 构建 + 全量测试**

Run: `export PATH=/Users/onyh/.nvm/versions/node/v22.22.1/bin:$PATH && node --check src/components/file-tree.js && node --check src/components/preview-window.js && node build.mjs && node --test`
Expected: check 通过；构建成功；72 全绿。

- [ ] **Step 6: 静态验证**

Run: `grep -c "data-wt-filter" client.js && grep -c "data-wt-preview-lineno" client.js && head -3 client.js`
Expected: 均 ≥1；首行 `window.__ModuleLoader__.load({`。

- [ ] **Step 7: Commit**

```bash
git add src/lib/tree-filter.js test/tree-filter.test.js src/lib/tokenize.js test/tokenize.test.js src/components/file-tree.js src/components/preview-window.js client.js
git commit -m "feat: file-tree filter + preview line numbers & lightweight syntax highlight"
```

---

## 里程碑验收清单（M3c）

- [x] `node --test` 全绿（**74 用例**：60 + git-history 8 + relativeTime 1 + tree-filter 2 + tokenize 2 + rpc-integration 1）。
- [x] host 端点 git.log/git.commit/git.reset/git.branch 单测（8，含 unborn-HEAD）+ files 校验集成测试。
- [x] client.js 重建提交（63KB）；静态验证标记齐全（data-wt-split/resize/commit-selected/history-row/op-error/filter/lineno）。
- [ ] **host 改动需重启 harness**；重启后 curl 探测 `git.branch`/`git.log`（controller 下一步执行）。
- [ ] GUI 手动验收：
  - [ ] 变更页签上下分区 + 分隔条拖拽；侧边栏宽度拖拽（200–600）；页签显示 `变更{n}` 徽章。
  - [ ] checkbox 多选 → 消息 → 提交选中；全部提交；预览选中（多文件 diff 拼接浮窗）。
  - [ ] 顶部 `📌 分支名`；消息框"▾ 最近"复用最近提交消息。
  - [ ] 已提交区历史列表（shortHash/消息/相对时间）；点击 → 回退（两段式确认 + 已推送风险提示）→ HEAD 回退且工作区改动保留。
  - [ ] 文件树过滤框按名过滤；文本预览显示行号 + 语法着色。
- [x] 推送至 GitHub 私有仓库 main。

## 后续（非 M3c 范围）

- reset hard / 变基 / push（YAGNI + 数据风险）；提交差异预览（选中提交的 stat）；预览更多语言高亮。
- **运维提醒**：git 写操作真实改动仓库——提交可回退撤销；回退仅 mixed 不丢改动。
